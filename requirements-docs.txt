# User documentation (MkDocs).
# Local build: pip install -r requirements-docs.txt, then `mkdocs serve`.
mkdocs-material>=9.5
mkdocs-static-i18n>=1.2
# Cards, timelines, merged-cell tables, project diagrams.
# The package ships no CSS of its own: it is vendored in docs/assets/css/.
neoteroi-mkdocs>=1.2
